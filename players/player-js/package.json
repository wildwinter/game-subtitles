{
  "name": "game-subtitles-player",
  "private": true,
  "description": "Runtime subtitle player for games — JS, browser-compatible",
  "type": "module",
  "main": "dist/game-subtitles-player.esm.js",
  "scripts": {
    "build":      "node build.mjs",
    "test":       "vitest run",
    "test:watch": "vitest",
    "demo":       "node build.mjs && node demo/serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest":  "^2.0.0",
    "jsdom":   "^24.0.0"
  }
}
